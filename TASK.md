# TASK — Tarugo

## Hoje (2026-03-25)
- [x] Ajustar organização de ripas em `consolidar_ripas()` para empilhar na altura (mantendo largura da ripa) e recalcular `ALTURA DA PEÇA` do “painel para ripas”.

## Discovered During Work
- [ ] Instalar `pytest` no ambiente atual para conseguir executar a suíte localmente (`python -m pytest` falhou com “No module named pytest”).

